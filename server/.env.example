# Copy to server/.env and fill in. Get both from https://dashboard.getstream.io
# -> your app -> Overview. The SECRET must NEVER ship inside the mobile app.
STREAM_API_KEY=qrp6sc4qxppt
STREAM_API_SECRET=your_stream_api_secret_here

# Port the directory service listens on (optional, defaults to 3000)
PORT=3000
